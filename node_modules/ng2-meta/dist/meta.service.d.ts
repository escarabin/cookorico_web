import { Title } from '@angular/platform-browser';
import { Router, ActivatedRoute } from '@angular/router';
import { Subscription } from 'rxjs/Subscription';
import 'rxjs/add/operator/filter';
export declare class MetaConfig {
    useTitleSuffix: boolean;
    defaults: any;
    constructor(config?: any);
}
export declare class MetaService {
    private titleService;
    private document;
    private router;
    private activatedRoute;
    private metaConfig;
    sub: Subscription;
    headElement: HTMLElement;
    constructor(titleService: Title, document: any, router: Router, activatedRoute: ActivatedRoute, metaConfig?: MetaConfig);
    private _getOrCreateMetaTag(name);
    private _updateMetaTags(meta?);
    setTitle(title?: string, titleSuffix?: string): void;
    setTag(tag: string, value: any): void;
}
