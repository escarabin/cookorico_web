"use strict";
var testing_1 = require('@angular/core/testing');
var slim_loading_bar_service_1 = require('../src/slim-loading-bar.service');
function main() {
    testing_1.describe('SlimLoadingBarService', function () {
        var service;
        testing_1.beforeEachProviders(function () {
            return [slim_loading_bar_service_1.SlimLoadingBarService];
        });
        testing_1.beforeEach(testing_1.inject([slim_loading_bar_service_1.SlimLoadingBarService], function (slbs) {
            service = slbs;
        }));
        testing_1.it('is defined', function () {
            testing_1.expect(slim_loading_bar_service_1.SlimLoadingBarService).toBeDefined();
            testing_1.expect(service instanceof slim_loading_bar_service_1.SlimLoadingBarService).toBeTruthy();
        });
        testing_1.it('starts at zero when just being injected', function () {
            testing_1.expect(service.progress).toBe(0);
        });
        testing_1.it('can change the progress to 30 if you call set progress', function () {
            service.progress = 30;
            testing_1.expect(service.progress).toBe(30);
        });
        testing_1.it('increaments over time after calling start()', testing_1.fakeAsync(function () {
            // var value, flag;
            testing_1.expect(service.progress).toBe(0);
            service.start();
            testing_1.tick(500);
            testing_1.expect(service.progress).toBe(1);
            service.stop();
        }));
        testing_1.it('have 100 returned from progress after complete()', function () {
            service.start();
            service.complete();
            testing_1.expect(service.progress).toBe(100);
        });
        testing_1.it('resets to zero when calling reset() after start() or set()', function () {
            service.progress = 30;
            service.reset();
            testing_1.expect(service.progress).toBe(0);
        });
        testing_1.it('will return 100 after calling complete', function () {
            service.progress = 30;
            service.complete();
            testing_1.expect(service.progress).toBe(100);
        });
        testing_1.it('return current height when calling height() without parameters', function () {
            testing_1.expect(service.height).toBe('2px');
        });
        testing_1.it('set the height when calling height() with parameter', function () {
            service.height = '5px';
            testing_1.expect(service.height).toBe('5px');
        });
        testing_1.it('return current color ', function () {
            testing_1.expect(service.color).toBe('firebrick');
        });
        testing_1.it('set the color', function () {
            service.color = 'green';
            testing_1.expect(service.color).toBe('green');
        });
    });
}
exports.main = main;
//# sourceMappingURL=service.spec.js.map